apiVersion: harbor-configuration.harbor.configuration/v1alpha1
kind: HarborConfiguration
metadata:
  name: harborconfiguration-quay
spec:
  harborTarget:
    name: harbor-cluster
    namespace: container-solutions
    harborUsername: admin
  registry:
    name: quay
    type: quay
    targetRegistryUrl: https://quay.io
    description: Endpoint created via crd
  projectReq:
    projectName: quay-project
    storageLimit: -1
    isPublic: true
  replication:
    name: quay-replication
    registryName: quay
    enablePolicy: true
    override: true
    destinationNamespace: quay-project
    filters:
    - type: name
      value: giantswarm/alpine
    trigger:
      type: manual